var s = [];
var arr = s;
for (let i = 0; i < 3; i++) {
    var pusher = {
        value: `item${i}`
    };
    var tmp;
    if (i !== 2) {
        tmp = [];
        pusher.children = tmpm
    }
    arr.push(pusher);
    arr = tmp;
}

console.log(s[0]);